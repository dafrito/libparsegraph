#include "parsegraph_environment.h"

parsegraph_EnvironmentStatus parsegraph_openCamera(apr_pool_t* pool, ap_dbd_t* dbd, int userId)
{
    return parsegraph_Environment_INTERNAL_ERROR;
}

parsegraph_EnvironmentStatus parsegraph_closeCamera(apr_pool_t* pool, ap_dbd_t* dbd, int userId)
{
    return parsegraph_Environment_INTERNAL_ERROR;
}
